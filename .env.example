# Product identity
APP_MODE=hosted
TEST_RUNNER=github-actions
APP_NAME=Failure intelligence
# Required outside Vercel. Vercel supplies its production URL automatically.
APP_BASE_URL=https://your-host

# GitHub OAuth app: callback URL is https://your-host/api/auth/callback/github
AUTH_SECRET=generate-with-pnpm-exec-auth-secret
AUTH_GITHUB_ID=replace-with-oauth-client-id
AUTH_GITHUB_SECRET=replace-with-oauth-client-secret
AUTHORIZATION_MODE=organization
AUTH_ALLOWED_ORGS=your-github-organization
AUTH_ALLOWED_USERS=
AUTH_TRUST_HOST=true

# AWS infrastructure created by the CDK stacks. Vercel obtains short-lived AWS
# credentials through OIDC; do not create a permanent AWS access key.
AWS_REGION=us-east-1
AWS_DYNAMODB_TABLE=rp-failure-intelligence
AWS_ROLE_ARN=replace-with-cdk-application-role-arn
DATA_ENCRYPTION_KEY=replace-with-at-least-32-random-characters
WEB_PUSH_PUBLIC_KEY=replace-with-vapid-public-key

# ReportPortal, TestRail, GitHub Actions/source, and Cypress environment credentials
# are configured per user in Settings.
# They are encrypted by the application before DynamoDB storage and are not deployment variables.
